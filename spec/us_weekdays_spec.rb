require 'spec_helper'

describe UsWeekdays do
  it 'has a version number' do
    expect(UsWeekdays::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
